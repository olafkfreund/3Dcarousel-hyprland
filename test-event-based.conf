# Test Event-Based 3D Carousel Plugin - Built Successfully
# Add this to your ~/.config/hypr/hyprland.conf

# Load the successfully built event-based plugin
plugin = /home/olafkfreund/Source/3Dcarousel-hyprland/hypr-carousel-events.so

# Test sequence - this version should work better:
# 1. Test compositor and rendering status
bind = SUPER_CTRL, T, exec, hyprctl dispatch carousel:test

# 2. Toggle carousel - should show both g_pHyprOpenGL and g_pHyprRenderer status
bind = SUPER, TAB, exec, hyprctl dispatch carousel:toggle

# 3. Navigate through carousel
bind = SUPER, right, exec, hyprctl dispatch carousel:next  
bind = SUPER, left, exec, hyprctl dispatch carousel:prev
bind = SUPER, RETURN, exec, hyprctl dispatch carousel:select
bind = SUPER, ESCAPE, exec, hyprctl dispatch carousel:exit

# Expected improvements in this version:
# - Checks both g_pHyprOpenGL AND g_pHyprRenderer before rendering
# - Uses official rendering patterns from borders-plus-plus
# - Better error handling with try-catch blocks
# - Should show "g_pHyprRenderer=OK" in debug output